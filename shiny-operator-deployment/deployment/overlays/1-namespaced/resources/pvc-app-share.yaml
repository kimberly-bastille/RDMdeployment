apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-share-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: ${APP_SHARE_SIZE}
  storageClassName: ""
  volumeName: pv-app-share
